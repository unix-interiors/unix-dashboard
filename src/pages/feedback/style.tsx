import { Box } from '@mui/material';
import { styled } from '@mui/material/styles';

export const Wrapper = styled(Box)`
  width: '100%';
`;

export const FeedbackTable = styled(Box)`
  background-color: #fff;
`;
